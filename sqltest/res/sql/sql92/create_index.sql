CREATE INDEX text_index ON test (itext);

CREATE INDEX text_index ON test (itext,iint);

CREATE INDEX text_index ON test (itext,iint,ifloat);

CREATE INDEX text_index ON test (itext,iint,ifloat,idouble);

CREATE INDEX int_index ON test (itext);

CREATE INDEX int_index ON test (itext,iint);

CREATE INDEX int_index ON test (itext,iint,ifloat);

CREATE INDEX int_index ON test (itext,iint,ifloat,idouble);

CREATE INDEX float_index ON test (itext);

CREATE INDEX float_index ON test (itext,iint);

CREATE INDEX float_index ON test (itext,iint,ifloat);

CREATE INDEX float_index ON test (itext,iint,ifloat,idouble);

CREATE INDEX double_index ON test (itext);

CREATE INDEX double_index ON test (itext,iint);

CREATE INDEX double_index ON test (itext,iint,ifloat);

CREATE INDEX double_index ON test (itext,iint,ifloat,idouble);

